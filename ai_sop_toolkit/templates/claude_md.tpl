# CLAUDE.md — 專案規範與禁止事項

## 核心準則
- 嚴格遵守 `TASK.md` 順序，一次只處理一個任務。
- 每次開發後必須進行雙軌驗證（自動化測試 + 人工 QA 指引）。
- **任務狀態更新規則**：
  - 開始任務 → 同步將 `TASK.md` 標記為 `[/]`，並在 `WORKLOG.md` 建立該任務條目
  - 完成任務 → 先填寫 `WORKLOG.md` 的測試過程與錯誤紀錄，再將 `TASK.md` 標記為 `[x]`

## WORKLOG.md 填寫規定
- 每個任務進入 `[/]` 時，必須在 `WORKLOG.md` 建立對應條目並填入負責人與開始時間。
- 測試失敗時，必須在「錯誤與修改紀錄」表格填入：發現時間、問題描述、修改內容、狀態。
- 未在 `WORKLOG.md` 完成紀錄前，**不得**將任務標記為 `[x]`。

## ❌ 禁止事項
- 不可跳過測試直接勾選任務。
- 不可在未釐清需求前直接更改核心架構。
- 嚴禁使用硬編碼 (Hard-coded) 敏感資訊。
- 不可在 `WORKLOG.md` 空白的情況下標記任務為 `[x]`。
